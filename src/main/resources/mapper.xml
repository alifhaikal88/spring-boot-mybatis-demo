<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xyz.alifhaikal88.mybatis.CoffeeMapper">
    <insert id="saveCoffee" parameterType="xyz.alifhaikal88.mybatis.Coffee"
            useGeneratedKeys="true"
            keyColumn="id"
            keyProperty="id">
        INSERT INTO coffee (name) VALUES (#{name})
    </insert>
</mapper>
